# Infrastructure

Local infrastructure for the demo app, including Postgres/PostGIS and any deterministic service fixtures needed for repeatable development and tests.

The root `docker-compose.yml` starts a PostGIS 16 container on `localhost:55432`.
