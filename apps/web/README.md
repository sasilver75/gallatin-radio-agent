# Web App

React/TypeScript operator UI for the Supply Officer View, Field Radio Console, Evidence Pane, COA review, and scenario playback.

The initial shell renders the Quarterback workspace and calls the API readiness endpoint at startup.

Run web tests from the repository root:

```sh
npm run test:web
```
